{
  "listeners": [{
    "proto": "http",
    "port": ${LISTENER_PORT},
    "mode": "epoll",
    "num_threads": ${N2K_NTHREADS},
    "decode_as": "${DECODER}"
  }],
  "brokers": "${KAFKA_BROKERS}",
  ${topic_config}
  ${kafka_opts}
}
