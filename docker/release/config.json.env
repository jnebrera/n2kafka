{
  "listeners": [{
    "proto": "http",
    "port": ${HTTP2K_PORT},
    "mode": "epoll",
    "num_threads": ${N2K_NTHREADS},
    "decode_as": "zz_http2k"
  }],
  "brokers": "${KAFKA_BROKERS}",
  ${kafka_opts}
}
